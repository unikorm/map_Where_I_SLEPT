
import styles from "../../styles/description.module.css";

const Description = () => {
    return (
        <section className={styles.descriptionSection}>
            <h2>On this map are places where I/We slept</h2>
            <p>In the map, there are marked places where I/we slept in a sleeping bag, in a tent, in a car, in a hotel, on a boat, and so on. I will try to also provide comments for those places, rate whether it was comfortable to sleep there, and so forth. I will refine this information over time.</p>
        </section>
    );
};

export default Description;